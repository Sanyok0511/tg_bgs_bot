server:
  port: 8081

telegram:
  configuration:
    name: botname
    token: token

spring:
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: create-drop
  datasource:
    username: elite
    password: elite
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/eliteTest
  liquibase:
    enabled: false
    change-log: config/liquibase/master.xml
  application:
    name: elite-tg-bot